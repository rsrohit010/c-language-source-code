#include<stdio.h>
int main()
{
  float r = 8.0;
  float y = 9.000;
  printf("the value of r is %f\n",r);
  printf("the value of y is %f\n",y);
  // we are adding a r+y flot
  float i = r+y;
  printf("the value of i is %f\n",i);
  // now we are multi[ply a float r*y;
  float k = r*y;
  printf("the value of k is %f\n",k);

  // now we are divide a float r/y
  float f = r/y;
  printf("the value of f is %f\n",f);

  return 0;

}
