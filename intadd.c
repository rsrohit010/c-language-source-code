#include<stdio.h>
int main()
{
  int a = 60;
  int b = 90;
  int c = 20;
  int z;
  z = b * a;
  printf("the value of z is %d\n",z);
  // now we are getting printf("%s\n", );
  printf("the value of a+b+c is %d\n",a+b+c);
  printf("the value of a-b-c is %d\n",a-b-c);
  printf("the value of a*b*c is %d\n",a*b*c);
  printf("the value of a/b/c is %d\n",a/b/c);
  return 0;
}
